{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "imagekit": "^3.0.4"
  },
  "scripts": {
    "start": "node server/index.js",
    "heroku-postbuild": "cd client && yarn && yarn run build",
    "install-packages": "yarn install && cd client && yarn install"
  }
}
